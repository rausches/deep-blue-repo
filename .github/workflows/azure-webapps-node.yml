name: Publish 🚀

on:
  workflow_dispatch:
  push:
    branches:
    - deploy-test

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0.x'
          
      - name: Restore
        run: dotnet restore ./Uxcheckmate.sln
        
      - name: Build
        run: dotnet build ./Uxcheckmate.sln --configuration Release --no-restore
        
      - name: Publish
        run: dotnet publish ./Uxcheckmate.sln --configuration Release --no-build
