@model List<Uxcheckmate_Main.Models.DesignIssue>

<h2 class="text-center">Design Issues Report</h2>

@if (Model == null || !Model.Any())
{
    <div class="alert alert-success">
        <strong>No UX issues found! Your site looks great.</strong>
    </div>
}
else
{
    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>Category</th>
                <th>Issue</th>
                <th>Severity</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var issue in Model)
        {
            <tr>
                <td>@(issue.Category?.Name ?? "Uncategorized")</td>
                <td>@issue.Message</td>
                <td>
                    @if (issue.Severity == 3)
                    {
                        <span class="badge bg-danger">High</span>
                    }
                    else if (issue.Severity == 2)
                    {
                        <span class="badge bg-warning">Medium</span>
                    }
                    else
                    {
                        <span class="badge bg-info">Low</span>
                    }
                </td>
            </tr>
        }
        </tbody>
    </table>
}



  @* <h3>Accessibility Issues</h3>
    <p>Total Issues: @Model.Item2.Count()</p>

    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>Code</th>
                <th>Message</th>
                <th>Selector</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var issue in Model.Item2 ?? Enumerable.Empty<Uxcheckmate_Main.Models.Pa11yIssue>())
        {
            <tr>
                <td>@issue.Code</td>
                <td>@issue.Message</td>
                <td>@issue.Selector</td>
            </tr>
        }
        </tbody>
    </table>*@ 
}

@section Scripts {
    <script src="~/js/reportResults.js"></script>
}